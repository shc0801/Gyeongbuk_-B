class Viewport{
    constructor(app, tool){
        // this.app = app;
        // this.tool = tool
        // this.width = this.app.canvas.width;
        // this.height = this.app.canvas.height;

        // this.toolList = new Array;
        // this.render();        
    }
 
    // render(){
    //     this.app.ctx.clearRect(0, 0, this.width, this.height);
    //     this.toolList.forEach(tool=>{
    //         tool.draw();
    //     })

    //     requestAnimationFrame(() => {
    //         this.render();        
    //     });
    // }
}